project
 - multipiece support
   -> field in type
   -> creation pop-up
   -> home page for project with multiple pieces
   -> piece creation pop-up

Counters
 - make secondary counters their own component
   -> keep map in same spot
   -> just have them in hteir own spot
   -> cause they will each have their own popup
 - add increase intervals
   -> fields in type
   -> creation popup
   -> increase support
   -> decrease support
 - settings pop-up
   -> add button to secondary counter component
   -> pop-up opens
   -> unlink/relink from global
   -> change reset number
   -> change interval of increase